import { ShiftPage } from '@/components/organisms/ShiftPage';
import { Props } from '@/types';
import React, { useState } from 'react';

const Page = async ({ params }: Props) => {
    return (
        <ShiftPage />
    );
};

export default Page;
